var circle2;
if (typeof require !== "undefined") {
  circle2 = require("../circle2.js");
} else {
  circle2 = window.circle2;
}

var ok = function(a, msg) { if (!a) throw new Error(msg || "not ok"); };

describe('circle2', function() {
  describe('#', function() {
    it('', function() {
      
    });
  });
});
